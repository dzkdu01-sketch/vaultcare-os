# 商品管理模块 MVP 开发前准备清单（已确认版）

> 日期：2026-03-04  
> 适用范围：PIM（商品管理）MVP 首期落地  
> 协作规则：业务优先提问、AI 预填、开发前门禁

---

## 0) 本期目标（预填）

**目标一句话（预填）**  
先打通并稳定 `PIM-F1`（AI 新增）、`PIM-F2`（手动新增）、`PIM-F3`（CSV 导入）三条关键流程，满足最小可验收闭环。

**本期不做（预填）**
- 不做 UI 体验重构（布局/动效/视觉）
- 不做价格版本策略（`PIM-F14`）
- 不做库存时效 SLA（`PIM-F15`）
- 不做大范围新功能扩展

---

## 1) 开发前门禁（必须全部通过）

| 门禁项 | 当前状态 | 证据/依据 | 结论 |
|---|---|---|---|
| 业务目标清晰 | 已清晰 | `docs/process-registry.md` 执行视图 + 本文目标 | ✅ 通过 |
| 范围边界清晰 | 基本清晰 | 圆桌共识“先闭环后体验” | ✅ 通过 |
| 验收标准清晰 | 部分清晰 | `docs/status/current-status.md` DoD 已列但未全部复测 | ⚠️ 待补证据 |
| P0 优先级明确 | 已清晰 | `PIM-F1/F2/F3` 均为 `P0` | ✅ 通过 |
| 执行负责人明确 | 已清晰 | 业务确认：本期技术 Owner = 老板（你） | ✅ 通过 |

**门禁结论（当前）**：`通过`（允许进入 `/开发`）

---

## 2) MVP 流程闭环清单（本期只做 3 条）

### 2.1 `PIM-F1` AI 辅助新增建档（P0）
- 业务结果：保存后必须为草稿、图片可见、品类正确
- 关键风险：误上架、图片丢失、品类错挂
- 当前状态：进行中（对应 Bug-02/03/04）
- 本期验收：通过后才允许进入“稳定可用”

### 2.2 `PIM-F2` 手动新增建档（P0）
- 业务结果：点击“手动新增”进入正确页面并完成保存
- 关键风险：入口错路由导致流程失效
- 当前状态：进行中（对应 Bug-01）
- 本期验收：路由正确 + 表单保存后详情展示正确

### 2.3 `PIM-F3` CSV 批量导入（P0）
- 业务结果：`master_code` 不存在时报错该行，其他行继续
- 关键风险：整批中断/结果不透明
- 当前状态：进行中（导入规则待按共识调整）
- 本期验收：成功数/失败数/失败明细可见

---

## 3) 角色与责任（已确认）

| 角色 | 本期职责（建议） | 状态 |
|---|---|---|
| 商品管理员 | 提交建档、处理导入失败行、复测关键流程 | 已确认 |
| 审核员 | 本期仅参与验收，不新增复杂审批动作 | 已确认 |
| 老板 | 只做优先级与上线窗口拍板 | 已确认 |
| 技术 Owner | 统筹 `F1/F2/F3` 实现与回归 | 已确认（老板/你） |

---

## 4) 字段与口径冻结（MVP 最小集）

**必须冻结（本期不得反复改口径）：**
- `is_active`：新建流程保存后为草稿
- `image_urls`：保存后非空且详情可见
- `primary_category`：必须是有效 FK 关联
- 导入结果：成功/失败统计 + 失败原因明细

**冻结结论**：口径已基本明确，可进入实现。

---

## 5) 验收与证据（双证据要求）

> 原则：`current-status` DoD + `system-functional-matrix` 状态必须一致。

### 本期最小验收包（建议）
1. `F1` 三条核心验收全部通过（草稿/图片/品类）
2. `F2` 路由与保存链路通过
3. `F3` 逐行继续导入规则通过
4. 复测结果回写到 `current-status.md`
5. 复测证据回写到 `system-functional-matrix.md`

**当前差距**：有 DoD 条目，但“已验证证据”不足。

---

## 6) 本周执行单（只做 3 件事）

1. 完成并验证 `PIM-F1`（Bug-02/03/04）
2. 完成并验证 `PIM-F2`（Bug-01）
3. 完成并验证 `PIM-F3`（导入逐行继续）

> 规则：本周不并行拉起第 4 件大事，避免多线程失焦。

---

## 7) 3 个业务决定（确认结果）

1. 本期只做 `F1/F2/F3`，其他顺延：**同意**  
2. 本期技术 Owner：**老板（你）**  
3. MVP 可用标准：**3 条流程全部通过**

---

## 8) 开工判定

- 若第 7 节三项已确认：**允许进入 `/开发`**
- 若未确认：**保持门禁，先补齐再开工**

**当前判定**：✅ **可开工（进入 `/开发 PIM-F1/F2/F3`）**
