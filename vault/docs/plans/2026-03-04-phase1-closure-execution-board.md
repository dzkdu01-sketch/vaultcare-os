# 阶段1闭环执行看板（人机协作落地版）

> 创建时间：2026-03-04  
> 目标：把“已开发但未闭环”转为“可验收、可追踪、可交接”的执行机制

---

## 1) 本轮唯一目标

在不扩范围的前提下，完成 `PIM-F1/F2/F13/F17/F18` 阶段1闭环证据，消除“功能有实现但无下文”的断档。

## 2) 本轮不做

- 不做 OCR 深化能力（复杂解析/表格理解）
- 不做 `PIM-F14/F15/F16` 深化改造
- 不做跨模块体验重构

## 3) 闭环定义（必须同时满足）

1. 页面行为证据：可复现、可截图/可录屏说明
2. 构建/测试证据：命令与结果可回填
3. 文档证据：`current-status` 与 `system-functional-matrix` 同步更新

---

## 4) 活跃任务卡（仅保留 1 张 in-progress）

| ID | 任务 | 优先级 | 状态 | 责任人 | ETA | 验收证据 | 阻塞类型 |
|----|------|--------|------|--------|-----|----------|----------|
| A | F17 导入口径前后端一致化 | P0 | 已完成待验收 | AI + 你确认 | 2026-03-04 | 导入说明文案 + 构建结果 + status 回填 | 无 |
| B | 阶段1页面行为验收补齐 | P1 | 进行中 | AI 执行，需你验页面 | 2026-03-05 | 统计条过滤/标签 chip/批量下架刷新证据 + matrix 回填 | 页面回归工作量 |
| C | F18 真实 WP 凭据成功链路复测 | P1 | 阻塞中 | 你提供凭据，AI 复测 | 待定 | 发布/撤销成功态页面证据 + status 回填 | 外部凭据/环境 |

> 规则：任何“完成”必须有证据链接；否则统一记为“已实现待闭环”。

---

## 5) 阻塞单模板（必须四要素）

复制以下模板逐条填写：

```md
### Blocker-<编号>
- 阻塞任务：<A/B/C>
- 阻塞点：<环境/凭据/权限/依赖/口径>
- 责任人：<姓名或角色>
- 预计解除时间：<YYYY-MM-DD HH:mm>
- 临时回归路径：<例如 simulate_success 或 mock>
- 下一步第一动作：<一个可立即执行动作>
```

---

## 6) 每日 2 次同步节奏（防断档）

- 12:00：更新任务状态（进行中/阻塞/待验收）
- 18:00：更新证据与文档回填状态
- 若 24 小时无更新：自动触发“续航包”

---

## 7) 你现在需要做的最小输入

1. 指定 `Task C` 凭据可用时间（或明确本周不可用）
2. 确认 `Task B` 页面验收顺序（统计条 -> 标签 -> 批量下架）
3. 指定阻塞责任人命名口径（真实姓名或角色名）

