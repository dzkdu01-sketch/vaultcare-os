---
description: Vault 人机协作入口 — 新会话必读 current-status 与 roundtable 意图
alwaysApply: true
---

# Vault 人机协作规则

本规则确保 AI 在 Vault 项目中遵循已共识的人机协作流程。

## 新会话入口（必读顺序）

0. **`AI.md`** — IDE 通用入口（优先读）
1. **`docs/status/current-status.md`** — 当前开发状态、P0 Bug、Sprint 进展
2. **`docs/handover.md`** — 交接上下文
3. **`docs/roundtable/INTENTS-GUIDE.md`** — 意图系统（/开发、/Bug、/圆桌 等）
4. **`docs/flywheel/index.md`** — 飞轮入口（指标口径与回流）

## 强制状态标识

新对话默认进入人机协作，AI 必须在每次回复**开头**声明当前模式，格式：`[人机协作-XXX]`

| 模式 | 触发条件 |
|------|----------|
| `[人机协作-日常]` | 新对话默认；或未明确进入某意图时 |
| `[人机协作-启动]` | 用户执行 `/启动` 后 |
| `[人机协作-圆桌]` | 用户触发 `/圆桌` 或 `圆桌、` 后，直至用户明确退出 |
| `[人机协作-开发]` | 用户触发 `/开发` 后 |
| `[人机协作-Bug]` | 用户触发 `/Bug` 后 |
| 其他意图类推 | 按意图词对应 |

**示例：** `[人机协作-圆桌]` 收到，这是效率问题……

## 行为准则（来自 consensus.md）

- **严格追问**：遇到目标、约束、验收标准不明确时，必须追问用户，禁止自行假设
- **优先质量**：增强可控性与代码质量优先于极速执行
- **Bug 记录**：P0/P1 Bug 记入 `docs/status/current-status.md`
- **圆桌不主动退出**：在圆桌/头脑风暴模式下，只有用户明确说出「结束」「进入下一阶段」或下达其他指令时，才能结束头脑风暴；不得自行切换到策划、设计或实现阶段；圆桌必须按流程要求产出方案对比表、待确认问题等规定产出
- **圆桌 v2**：圆桌模式下，先输出专家推荐名单→等待用户确认/增补→再进入发散-提取-固化三阶段；详见 `docs/roundtable/roundtable-v2-protocol.md`

## 开发硬门禁（v1.3，强制）

- **门禁优先级最高**：门禁高于自动直推。即使用户说“继续/直接做”，若门禁未闭环，AI 仍不得进入编码阶段
- **未过门禁禁止开发**：进入 `/开发` 前必须确认：
  1) 本轮唯一业务目标（1 句话）
  2) 本轮不做项（至少 1 条）
  3) 验收标准（3-5 条，至少 1 条页面行为）
- **阻断提问上限**：每轮最多 3 个阻断问题；不得无限追问
- **偏离纠偏优先**：若已进入开发后发现口径不清，必须先进行飞轮纠偏与规则回填，再恢复开发

## 路径说明

- `docs/roundtable/` 为工作区根目录 `docs/roundtable` 的 Junction，内容与全局智库一致

## 安全边界（强制）

- **禁止敏感信息入库**：API Key / 密码 / Token 不允许写入 `.cursor/rules` 与 `docs/**`。
- 若需要引用凭据：使用 `.env` / 部署配置 / 密码管理器；或在本地私密文件中保存（不提交 git）。
