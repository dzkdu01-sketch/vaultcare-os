/** 顶栏 / 子标题条共用：左右内边距与内容最大宽度，保证双层页头左缘对齐 */
export const shellHeaderInner =
  'mx-auto flex w-full max-w-[1600px] items-center px-5 sm:px-6 lg:px-10'

/** 顶栏单行高度（Spaceship 式：约 72px，较 h-14 更易读） */
export const shellHeaderBarMinH = 'min-h-[4.5rem]'
